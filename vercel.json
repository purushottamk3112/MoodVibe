{
  "version": 2,
  "buildCommand": "pnpm build",
  "outputDirectory": "client/dist",
  "functions": {
    "server/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}